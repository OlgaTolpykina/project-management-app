import { FilterExecPipe } from './filter-exec.pipe';

describe('FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExecPipe();

    expect(pipe).toBeTruthy();
  });
});
